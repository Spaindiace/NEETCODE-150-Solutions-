class Solution {
public:
    int search(vector<int>& nums, int target) {
        int n = nums.size();
        bool found = false;
        for (int i = 0; i < n; i++) {
            if (nums[i] == target) {
                found = true;
                return i;
            }
        }
        return -1;
    }
};
